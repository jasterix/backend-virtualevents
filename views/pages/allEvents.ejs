<div class="jumbotron text-center">
  <h1>All Events</h1>
</div>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Event Date</th>
      <th>Title</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <% for (let event of events) { %> <%let options = { weekday: 'long', year:
    'numeric', month: 'long', day: 'numeric', hour: "numeric", minute: 'numeric'
    }%>

    <tr>
      <td><h6><%=event.startDate%></h6></td>
      <td><h5><%= event.title %></h5></td>

      <td class="description"><%= event.description %></td>
      <td>
        <a href="/events/<%= event.id %>" class="btn btn-sm btn-primary"
          >View Event</a
        >
      </td>

      <td>
        <form action="/events/delete/<%= event.id %>" method="post">
          <button type="submit" class="btn btn-sm"">Delete Event</button>
        </form>
      </td>
    </tr>

    <!-- <td>
    <a href="/events/<%= event.id %>" class="btn btn-sm btn-primary"
          >View</a
        >
        <a href="/events/<%= event.id %>/edit" class="btn btn-sm btn-primary"
          >Edit</a
        >
        <a href="/events/<%= event.id %>/delete" class="btn btn-sm btn-danger"
          >Delete</a
        >
      </td>
    </td> -->

    <% } %>
  </tbody>
</table>
